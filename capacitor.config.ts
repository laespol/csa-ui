import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'gad.santoamerico.org.br',
  appName: 'csa-uin',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
